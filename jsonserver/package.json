{
  "name": "jsonserver",
  "version": "1.0.0",
  "description": "JSON Server for product API",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch data.json --port 3001"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}